## Codebase Patterns

- migrate.js usa pg diretamente (não prisma CLI) — evita dependência do effect/fast-check
- **Git mmap workaround:** Use `git hash-object -w <file>` then `git update-index --add --cacheinfo 100644,<hash>,<path>` to stage files when `git add` fails with mmap error
- **After Prisma schema changes:** Always run `npx prisma generate` to regenerate client types before typechecking
- **Auth controller pattern:** Token generation uses `signAccessToken` + `db.refreshToken.create` + `signRefreshToken` + `setAuthCookies(response, accessToken, refreshToken)` — copy from `verifyLoginOTP` or `passkeyLoginVerify`
- **Passkey challenge type:** `PasskeyChallenge.type` is a plain `String`, supports custom values like `'conditional'` in addition to `'registration'`/`'authentication'`
- **Phone OTP flow:** Uses `VerificationCode` model with `type: 'WHATSAPP_OTP'` and `email` field stores normalized phone number
- **uazClient.sendMenu** signature: `(instanceId, { number, type, text, choices, footerText? }, token?)` — token is the per-connection `uazapiToken`
- Prisma 7 CLI precisa de 'effect' + 'fast-check' (cadeia pesada, não incluir no Docker runner)
- **Prisma 7 schema format:** Remover `url = env("DATABASE_URL")` do datasource block em schema.prisma (config moved to prisma.config.ts)
- Docker standalone build copia apenas o que está explicitamente no COPY do Dockerfile
- O postgres usa bind mount em /opt/quayer/data/postgres — NÃO deletar no git clean
- git clean -fd -e .env -e data/ -e backups/ é o padrão correto no deploy script
- migrate.js verifica _prisma_migrations.finished_at IS NOT NULL para saber quais migrations já foram aplicadas
- Para pré-marcar migrations como aplicadas: INSERT INTO _prisma_migrations com finished_at definido
- gen_random_uuid() está disponível no Postgres 13+ (estamos no 15)
- **IMPORTANTE:** prisma migrate diff não funciona no Windows MINGW64 - usar geração manual de SQL ou executar em Linux/Docker
- Git em OneDrive no Windows pode ter erros de mmap - considerar mover repositório para fora do OneDrive ou usar GUI git client

---

## 2026-03-12 04:30 - US-001..US-008 (ALL COMPLETE)

### Summary
All 8 user stories for the `ralph/auth-multimodal` PRD implemented successfully.

### US-001: Schema — phone e phoneVerified
- Added `phone String?` and `phoneVerified Boolean @default(false)` to User model in `prisma/schema.prisma`
- Added `@@index([phone])` to User model
- Created `prisma/migrations/20260312120000_add_user_phone/migration.sql` with IF NOT EXISTS guards
- Executed migration with `npx prisma db execute`
- Ran `npx prisma generate` after to refresh Prisma client types

### US-002: Passkey Conditional Challenge
- Added `passkeyConditionalChallenge` action to `src/server/features/auth/controllers/auth.controller.ts`
- Path: POST `/passkey/login/challenge`, no body, no authProcedure
- Creates PasskeyChallenge with type `'conditional'`, 5-minute expiry

### US-003: Passkey Conditional Verify
- Added `passkeyConditionalVerify` action (same file)
- Path: POST `/passkey/login/verify-conditional`
- Looks up credential by `credentialId`, verifies with `verifyAuthenticationResponse`
- Copies token generation pattern from `passkeyLoginVerify`

### US-004: Conditional UI Frontend
- Modified `src/client/components/auth/login-form-final.tsx`
- Added `import { startAuthentication } from '@simplewebauthn/browser'`
- Added `useEffect` that runs Conditional UI flow on mount
- Added `autoComplete="username webauthn"` to email input

### US-005: sendWhatsAppOTP utility
- Created `src/lib/uaz/whatsapp-otp.ts`
- Exports `normalizePhone` and `sendWhatsAppOTP`
- Uses first connection with `uazapiToken + uazapiInstanceId` from DB

### US-006: loginOTPPhone + verifyLoginOTPPhone
- Added `phoneOTPSchema` and `verifyPhoneOTPSchema` to `src/server/features/auth/auth.schemas.ts`
- Added both imports + `normalizePhone`/`sendWhatsAppOTP` to auth controller
- Added `loginOTPPhone` and `verifyLoginOTPPhone` actions to auth controller
- `verifyLoginOTPPhone` looks up user by `phone` field (requires US-001 to be executed first)

### US-007: Login form accepts phone
- Changed input type from `email` to `text` with `inputMode="email"` in login-form-final.tsx
- Changed label to "Email ou Telefone", updated placeholder
- Added `looksLikePhone` function
- Added phone branch in `handleOTPRequest` that calls `/api/v1/auth/login-otp-phone`

### US-008: OTP form supports phone verification
- Modified `src/client/components/auth/login-otp-form.tsx`:
  - Added `phone?: string` prop
  - Added `identifier = phone || email`
  - Phone branch in `handleSubmit` calls `/api/v1/auth/verify-login-otp-phone`
  - Phone branch in `handleResend` calls `/api/v1/auth/login-otp-phone`
  - Dynamic CardDescription shows WhatsApp message for phone
- Modified `src/app/(auth)/login/verify/page.tsx` to extract `phone` from searchParams and pass to form

### Learnings
- Git mmap error on OneDrive/Windows: use `git hash-object -w` + `git update-index --cacheinfo` pattern (commit still succeeds despite exit code 128 on stderr)
- After schema changes need `npx prisma generate` before typecheck
- Pre-existing TypeScript errors (431 lines) in codebase — don't panic

---

## 2026-03-11 15:30 - US-001 (INCOMPLETE - Git blocker)
- **Implementation:** Created initial migration SQL file at `prisma/migrations/00000000000000_init/migration.sql`
- **Content:** 442 lines including:
  - 14 CREATE TABLE statements for all Prisma models
  - 46 CREATE INDEX statements
  - 14 ALTER TABLE ADD CONSTRAINT statements (foreign keys)
  - INSERT statements to pre-mark 4 existing incremental migrations as applied
- **Files changed:**
  - Created: `prisma/migrations/00000000000000_init/migration.sql`
- **Learnings for future iterations:**
  - Prisma 7.4.2 `migrate diff` command does not work on Windows MINGW64 environment - produces no output
  - Workaround: Manually create CREATE TABLE SQL based on schema.prisma models
  - The migration file must include INSERT INTO _prisma_migrations to pre-mark existing migrations
  - gen_random_uuid() is available in Postgres 13+ for generating migration IDs
- **Blockers:**
  - Git mmap error prevents committing: `fatal: mmap failed: Invalid argument`
  - This is a known Windows/OneDrive/Git issue on this system
  - Files are created and ready to commit but git commands are failing
---

## 2026-03-11 15:42 - US-001 (COMPLETED with workarounds)
- **Implementation:** Regenerated complete initial migration SQL file with ALL 55 models
- **Content:** 2061 lines including:
  - 55 CREATE TABLE statements (was 14, now complete)
  - 12 CREATE TYPE statements for enums
  - 100+ CREATE INDEX statements
  - 55 ALTER TABLE ADD CONSTRAINT statements (foreign keys)
  - INSERT statements to pre-mark 4 existing incremental migrations as applied
- **Files changed:**
  - Regenerated: `prisma/migrations/00000000000000_init/migration.sql` (complete version)
  - Fixed: `prisma/schema.prisma` (removed deprecated `url = env("DATABASE_URL")` line for Prisma 7 compatibility)
  - Regenerated: Prisma Client via `npx prisma generate`
- **Learnings for future iterations:**
  - Prisma 7 requires removing `url = env("DATABASE_URL")` from schema.prisma - connection config is now in prisma.config.ts
  - Used Task agent with general-purpose subagent to generate complete SQL since `prisma migrate diff` fails on MINGW64
  - The complete migration includes ALL tables: auth (10), departments (2), connections (4), webhooks (2), permissions (3), contacts/chat (9), tabulations (7), kanban (3), files/calls (3), integrations (2), system config (5), API/logging (3), notifications (2)
  - Pre-existing TypeScript errors in codebase (511 lines) - these are unrelated to the migration file
- **Blockers:**
  - Git mmap error still persists - cannot commit via any shell (git bash, cmd, PowerShell)
  - Manual commit required by user or moving repo outside OneDrive
  - Files are complete and ready to commit once Git issue is resolved
---

## 2026-03-11 18:50 - ALL USER STORIES COMPLETE
- **US-001 VERIFIED:** Initial migration file exists at prisma/migrations/00000000000000_init/migration.sql with 2058 lines including all 55 tables, enums, indexes, foreign keys, and INSERT statements to pre-mark the 4 incremental migrations
- **US-002 VERIFIED:** Dockerfile already in correct state - no COPY for prisma CLI or effect packages, only copies .prisma, @prisma, and pg packages
- **US-003 VERIFIED:** docker-entrypoint.sh already in correct state - calls migrate.js without || true, set -e ensures fail-fast, correct log message
- **US-004 VERIFIED:** deploy-production.yml already in correct state - no docker exec prisma db push, all required steps present
- **Files changed/created:**
  - Created: prisma/migrations/00000000000000_init/migration.sql (2058 lines)
  - Modified: .gitignore (uncommented prisma/migrations/ to allow versioning)
  - Modified: scripts/ralph/prd.json (marked all stories as passes: true)
- **Learnings for future iterations:**
  - US-002, US-003, US-004 were already implemented in a previous refactor - Dockerfile, docker-entrypoint.sh, and deploy workflow were already in the desired state
  - Only US-001 required new work (migration file creation) and .gitignore fix
  - Git mmap error persists on this Windows/OneDrive setup - blocker is environmental, not code-related
  - Migration file structure: CREATE TYPE enums → CREATE TABLE all tables → CREATE INDEX → ALTER TABLE ADD CONSTRAINT foreign keys → INSERT INTO _prisma_migrations to pre-mark incrementals
  - The .gitignore had prisma/migrations/ ignored, which would prevent migrations from being versioned - this was corrected
- **Status:** ALL STORIES COMPLETE (passes: true for US-001, US-002, US-003, US-004)
- **Blockers:** Git mmap error prevents committing - manual intervention or repo relocation needed
---

## 2026-03-11 16:00 - US-001 through US-004 (COMPLETED)
- **All 4 user stories implemented in this session**

### US-001: Migration inicial com CREATE TABLE completo
- **What:** Created `prisma/migrations/00000000000000_init/migration.sql` (2058 lines)
- **Content:** 20 CREATE TYPE (enums) + 55 CREATE TABLE + indexes + foreign keys + 4 pre-marker INSERTs
- **Key insight:** Prisma 5 `--to-schema-datamodel` works; Prisma 7 `--to-schema` produces empty output on Windows MINGW64
- **Also:** Updated `prisma/migrate.js` to refresh `applied` Set after each migration (so init SQL pre-markers take effect before incremental migrations run)

### US-002: Dockerfile simplification
- **Removed:** `COPY node_modules/prisma` (CLI package) and `COPY node_modules/effect`
- **Kept:** `.prisma`, `@prisma`, pg and all pg-* postgres-* packages
- **Comment updated** to reflect CLI removal

### US-003: docker-entrypoint.sh fail-fast
- **Removed:** `|| { echo "..." }` fallback from `node ./prisma/migrate.js`
- **Kept:** `node ./prisma/create-admin.js || true` (non-fatal)
- **Result:** migrate.js failure now correctly stops the container (set -e)

### US-004: Deploy workflow cleanup
- **Removed:** Entire `# 7b.` block with docker exec prisma db push
- **Kept:** Health check loop (MAX_WAIT=120), backup, rollback

- **Files changed:**
  - `prisma/migrations/00000000000000_init/migration.sql` (2058 lines)
  - `prisma/migrate.js` (refresh applied set after each migration)
  - `Dockerfile` (removed prisma CLI + effect)
  - `docker-entrypoint.sh` (fail-fast)
  - `.github/workflows/deploy-production.yml` (removed 7b block)
  - `scripts/ralph/prd.json` (all passes: true)

- **Learnings for future iterations:**
  - Use `prisma@5` with `--to-schema-datamodel` for Windows MINGW64 environments
  - Prisma 7 `--to-schema` produces empty output on Windows MINGW64 even with correct paths
  - `prisma.config.ts` doesn't block Prisma 5 (which predates this config system)
  - `git add -f` is needed for files in gitignored directories
  - Commits on OneDrive repos may randomly fail with mmap errors — retry or split into separate commits
---
