## Codebase Patterns

- **Cookie name:** auth usa `accessToken` (não `access_token`) — setCookie em auth.controller.ts line ~89
- **Client components:** NUNCA importar `@/lib/auth` (barrel) em `use client` — importar direto de `@/lib/auth/auth-provider`
- **PhoneInput:** usa react-international-phone + usePhoneInput hook; forwardRef expondo ref do input nativo; `forceDialCode: true` mantém Brasil selecionado
- **looksLikePhone:** `const clean = v.replace(/[^\d]/g, ""); return clean.length >= 8` — funciona com E.164 e nacional
- **normalizePhone backend:** `phone.replace(/\D/g, "")` strip tudo — aceita qualquer formato
- **UAZAPI_INSTANCE_TOKEN env:** fallback quando nenhuma connection no DB tem uazapiToken
- **igniter schema:** regenerar manualmente quando CLI falha (ajv/dist/core error)
- **apiFetch helper:** retorna response.json() raw — data.data é o array
- **Git mmap workaround:** Use `git hash-object -w <file>` then `git update-index --add --cacheinfo` when `git add` fails. For commits: rebuild index with `git ls-tree -r HEAD`, then `git write-tree` + `git commit-tree` + `git update-ref`
- **Prisma migrate shadow DB issues:** When `prisma migrate dev` fails with shadow DB errors, create migration SQL manually and use `prisma migrate deploy`. Mark existing migrations as applied with `prisma migrate resolve --applied <name>`
- **Prisma v7 datasource:** URL goes in `prisma.config.ts`, NOT in schema.prisma `datasource` block. Remove `url = env("DATABASE_URL")` from schema
- **Corrupted git objects:** `.cursor/rules` tree object `fdc1aea` is missing from packfile. Workaround: skip it during index rebuild, use `git ls-tree -r HEAD 2>&1 | grep -v "^error:"`
- **dev server:** roda em http://localhost:3000 — verificar com `curl -s http://localhost:3000/api/health`
- **Tailwind dark mode / CSS vars:** globals.css define vars como `oklch(...)` diretamente (NÃO como valores de canal HSL). Ao usar inline styles, usar `var(--popover)` etc. diretamente — NUNCA `hsl(var(--popover))` pois quebra o color. Em classes Tailwind usar `bg-popover`, `text-popover-foreground` etc.
- **FieldSeparator bg:** usa `bg-card` (não `bg-background`) pois o separador fica dentro de um Card — em dark mode card e background têm cores diferentes
- **WCAG 2.1 AA:** contraste mínimo 4.5:1 texto normal, 3:1 elementos UI; touch target mínimo 44×44px; focus-visible em todos elementos interativos
- **Shadcn components path:** componentes ficam em `src/client/components/ui/` — usar esses, não criar novos
- **Igniter data pattern:** `const { data } = api.x.y.useQuery()` — data pode ser `{ data: [...] }` ou array direto dependendo do controller
- **Array guard pattern:** `Array.isArray(data?.data) ? data.data : Array.isArray(data) ? data : []` — previne `.filter is not a function`
- **npm install:** always use `--legacy-peer-deps` flag due to peer dependency conflicts
- **crypto.ts AES-256-GCM:** `encrypt(plaintext)` / `decrypt(encrypted)` in `src/lib/crypto.ts` — reuse for any secret storage
- **otpauth validate:** `totp.validate({ token, window })` returns `null` if invalid, integer delta if valid
- **otpauth Secret from stored:** `OTPAuth.Secret.fromBase32(base32)` to reconstruct from DB, NOT `new OTPAuth.Secret()`
- **Prisma $transaction array:** `db.$transaction([op1, op2])` for atomic multi-table updates
- **verifyPassword reuse:** `verifyPassword(plain, hash)` from `@/lib/auth/bcrypt` wraps `bcrypt.compare` — use for any hash comparison (passwords, recovery codes, etc.)
- **Token issuance flow:** signAccessToken → db.refreshToken.create → signRefreshToken → update → setAuthCookies → registerDeviceSession — duplicated in login/totpChallenge/totpRecovery
- **2FA status check (frontend):** `/api/v1/auth/me` does NOT return `twoFactorEnabled` — use `/api/v1/auth/totp/devices` and check for verified devices instead
- **Igniter extra body fields:** procedure-consumed fields (like `cf-turnstile-response`) aren't in Zod schema — use `as any` cast on body object in typed `api.*.mutate()` calls
- **CustomRole permissions cache:** `getCustomRolePermissions(id, db)` in `src/lib/auth/permissions.ts` — in-memory Map with 5min TTL. Call `invalidateCustomRoleCache(id)` when CRUD-ing CustomRoles
- **hasPermission with CustomRole:** `hasPermission(orgRole, resource, action, customRolePerms?)` — 4th param is optional; if provided, checks CustomRole JSON instead of PERMISSIONS_MATRIX
- **authProcedure customRole context:** `context.auth.customRole` is `{ id, slug, permissions, priority } | null` — resolved from `UserOrganization.customRoleId` of current org
- **apiFetch helper pattern:** wrap `fetch()` with `{ credentials: 'include', headers: { 'Content-Type': 'application/json' } }` + error extraction from response body
- **Igniter 429 response:** `response` object has no `tooManyRequests` method — use `return Response.json({ error: '...' }, { status: 429 })` directly
- **dns.resolveTxt():** returns `string[][]` — use `.flat()` to get flat array of TXT records before searching

---

# Ralph Progress Log

## Previous PRD: UX Audit All Pages (ralph/ux-audit-all-pages)
Archived to: scripts/ralph/archive/prd-ux-audit-all-pages.json
US-001 and US-002 completed. Remaining stories paused.

---

## New PRD: Auth Platform Hardening (ralph/auth-platform-hardening)
Started: Thu Mar 13 2026
Scope: MFA/TOTP, Bot Detection (Turnstile), Custom Roles, Verified Domains, SCIM Provisioning
Phases: 1-Security (US-001→010), 2-Governance (US-011→017), 3-Enterprise (US-018→020)
---

## 2026-03-13 ~03:30 UTC-3 - US-001
- **Implemented:** Schema e modelos Prisma para TOTP 2FA
- **Changes:**
  - Added `twoFactorEnabled Boolean @default(false)` to User model
  - Added `TotpDevice` model (id, userId, secret, name, verified, createdAt) with `@@map("totp_devices")`
  - Added `RecoveryCode` model (id, userId, code, usedAt, createdAt) with `@@map("recovery_codes")`
  - Added relations: User hasMany TotpDevice, User hasMany RecoveryCode (both with onDelete: Cascade)
  - Added indexes: `@@index([userId])` on both new models
  - Created migration `20260313200000_add_totp_2fa` manually (shadow DB unavailable)
  - Applied migration with `prisma migrate deploy`
  - Removed deprecated `url = env("DATABASE_URL")` from schema datasource (Prisma v7 uses prisma.config.ts)
- **Files changed:**
  - `prisma/schema.prisma` — added field + 2 models + relations
  - `prisma/migrations/20260313200000_add_totp_2fa/migration.sql` — new migration
- **Learnings for future iterations:**
  - Git repo has corrupted `.cursor/rules` tree object — must use low-level git commands for all operations
  - `git checkout` between branches fails with mmap error — use `git symbolic-ref HEAD` + `git read-tree` as workaround
  - Pre-existing TS errors in `admin/actions.ts`, `EmailSettings.tsx`, etc. — not related to schema changes, should not block
  - Prisma v7 removed `url` from datasource block in schema — connection config lives in `prisma.config.ts`
  - Shadow database migration replay fails due to inconsistent migration history — use `migrate deploy` + `migrate resolve` instead
---

## 2026-03-13 ~03:30 UTC-3 - US-002
- **Implemented:** Endpoint totpSetup — gerar secret e QR code
- **Changes:**
  - Installed `otpauth`, `qrcode`, `@types/qrcode` packages (--legacy-peer-deps required)
  - Added `totpSetupSchema` to `src/server/features/auth/auth.schemas.ts`
  - Added `totpSetup` mutation action to `src/server/features/auth/controllers/auth.controller.ts`
  - Uses existing `src/lib/crypto.ts` encrypt() for AES-256-GCM encryption of TOTP secret
  - Uses existing `hashPassword()` from `src/lib/auth/bcrypt.ts` for recovery code hashing
  - Rate limited via `authRateLimiter` with `totp-setup:` prefix
  - Cleans up incomplete setups (unverified devices + unused recovery codes) before new setup
- **Files changed:**
  - `package.json` / `package-lock.json` — added otpauth, qrcode, @types/qrcode
  - `src/server/features/auth/auth.schemas.ts` — added totpSetupSchema
  - `src/server/features/auth/controllers/auth.controller.ts` — added totpSetup action + imports
- **Learnings for future iterations:**
  - `npm install` requires `--legacy-peer-deps` flag due to peer dependency conflicts in this repo
  - crypto.ts already has AES-256-GCM encrypt/decrypt — reuse it, no need to create new helpers
  - Auth controller actions follow pattern: `igniter.mutation({ name, description, path, method, use, body, handler })`
  - Pre-existing TS errors (~60+) in unrelated files — do not block commits, verified no new errors in changed files
  - `otpauth` library: `new OTPAuth.TOTP({ secret: new OTPAuth.Secret() })` for generation, `.toString()` for URI
---

## 2026-03-13 ~04:00 UTC-3 - US-003
- **Implemented:** Endpoint totpVerify — confirmar setup de 2FA
- **Changes:**
  - Added `totpVerifySchema` to `src/server/features/auth/auth.schemas.ts` with Zod validation (code: 6 digits, deviceId: uuid)
  - Added `totpVerify` mutation action to `src/server/features/auth/controllers/auth.controller.ts`
  - Imports `decrypt` from `@/lib/crypto` to recover encrypted TOTP secret from DB
  - Reconstructs `OTPAuth.TOTP` with `OTPAuth.Secret.fromBase32(decryptedSecret)` and validates with `window: 1`
  - Uses `db.$transaction` for atomic update of TotpDevice.verified + User.twoFactorEnabled
  - Rate limited via `authRateLimiter` with `totp-verify:` prefix (5 attempts / 15 min)
  - Checks device ownership (userId match), device not already verified, and device exists
  - Console audit log: `totp_setup_completed`
- **Files changed:**
  - `src/server/features/auth/auth.schemas.ts` — added totpVerifySchema + TotpVerifyInput type
  - `src/server/features/auth/controllers/auth.controller.ts` — added totpVerify action + decrypt import
- **Learnings for future iterations:**
  - `OTPAuth.Secret.fromBase32(base32String)` to reconstruct secret from stored base32 (not `new OTPAuth.Secret({ buffer })`)
  - `totp.validate({ token, window })` returns `null` if invalid, or a delta integer if valid — check `=== null` for failure
  - `db.$transaction([...])` accepts array of Prisma operations for atomic multi-table updates
---

## 2026-03-13 ~05:00 UTC-3 - US-004
- **Implemented:** Fluxo de login com 2FA challenge
- **Changes:**
  - Added `totpChallengeSchema` to `src/server/features/auth/auth.schemas.ts` (challengeId + 6-digit code)
  - Added helper functions to auth controller: `sign2faChallenge()`, `verify2faChallenge()`, in-memory `challengeAttempts` Map with auto-cleanup
  - Modified `login` action: after password verification + active check, if `user.twoFactorEnabled` → return `{ requiresTwoFactor: true, challengeId }`
  - Modified `googleCallback` action: after user lookup/creation, if existing user has 2FA → return challenge (skip for new users)
  - Modified `verifyMagicLink` action (login path): after active check, if 2FA enabled → return challenge
  - Modified `verifyLoginOTPPhone` action: after phone OTP verified, if existing user has 2FA → return challenge (skip for newly created)
  - Added `totpChallenge` mutation action: validates challengeId JWT (5min expiry), decrypts TOTP secret, validates code with window=1, issues tokens on success
  - Attempt tracking: in-memory Map per challengeId, max 5 attempts, auto-cleanup after 10 minutes
  - Audit logs: `2fa_challenge_issued`, `2fa_challenge_success`, `2fa_challenge_failed`
- **Files changed:**
  - `src/server/features/auth/auth.schemas.ts` — added totpChallengeSchema + TotpChallengeInput type
  - `src/server/features/auth/controllers/auth.controller.ts` — added 2FA challenge helpers, modified 4 login actions, added totpChallenge action
- **Learnings for future iterations:**
  - `jwt.sign({ userId, type: '2fa-challenge' }, secret, { expiresIn: '5m' })` — use short-lived JWTs for challenge tokens instead of DB storage
  - In-memory Map with `setTimeout` auto-cleanup is simple and effective for short-lived challenge attempt tracking
  - When modifying multiple login flows, the 2FA check must go AFTER first-factor validation but BEFORE token issuance
  - For Google OAuth and phone OTP login, skip 2FA check for newly created users (they can't have 2FA configured yet)
  - Pre-existing TS errors (~60+) in unrelated files continue — confirmed no new errors from US-004 changes
---

## 2026-03-13 ~06:00 UTC-3 - US-005
- **Implemented:** Login com Recovery Code — totpRecovery action
- **Changes:**
  - Added `totpRecoverySchema` to `src/server/features/auth/auth.schemas.ts` (challengeId + recoveryCode)
  - Added `totpRecovery` mutation action to `src/server/features/auth/controllers/auth.controller.ts`
  - Verifies challengeId JWT using same `verify2faChallenge()` helper as totpChallenge
  - Shares in-memory `challengeAttempts` map (max 5 attempts per challengeId)
  - Fetches all unused recovery codes (`usedAt: null`) and compares with `verifyPassword` (bcrypt.compare)
  - On match: marks code as used (`usedAt = now()`), issues tokens (same flow as totpChallenge)
  - Returns `remainingCodes` count in response
  - If `remainingCodes <= 2`: includes `warning: 'few_codes_remaining'`
  - If `remainingCodes === 0`: disables 2FA (`twoFactorEnabled=false`), deletes all TotpDevices, sends email alert
  - Email alert uses `emailService.send()` with HTML body explaining 2FA was auto-disabled
  - Audit logs: `2fa_recovery_used`, `2fa_recovery_failed`, `2fa_auto_disabled`
- **Files changed:**
  - `src/server/features/auth/auth.schemas.ts` — added totpRecoverySchema + TotpRecoveryInput type
  - `src/server/features/auth/controllers/auth.controller.ts` — added totpRecovery action + totpRecoverySchema import
- **Learnings for future iterations:**
  - `verifyPassword()` from `@/lib/auth/bcrypt` is a generic bcrypt.compare wrapper — reusable for any hash comparison, not just passwords
  - Recovery code comparison must iterate all unused codes since we can't look up by plaintext (hashed storage)
  - The token issuance flow (signAccessToken → create refreshToken → update → setAuthCookies → registerDeviceSession) is duplicated across totpChallenge/totpRecovery — consider extracting a helper in future
---

## 2026-03-13 ~07:00 UTC-3 - US-006
- **Implemented:** Gerenciamento 2FA — desabilitar, regenerar codes, listar devices
- **Changes:**
  - Added `totpDisableSchema` and `totpRegenerateCodesSchema` to `src/server/features/auth/auth.schemas.ts`
  - Added `totpDisable` mutation action: verifies password (bcrypt), then verifies TOTP code OR recovery code as alternative, deletes all TotpDevices + RecoveryCodes, sets `twoFactorEnabled=false`
  - Added `totpRegenerateCodes` mutation action: verifies TOTP code with window=1, invalidates existing codes (`usedAt=now()`), generates 8 new codes hashed with bcrypt, returns plaintext
  - Added `totpListDevices` query action: returns user's TotpDevices with `id, name, verified, createdAt`
  - Audit logs: `totp_disabled`, `totp_codes_regenerated`
  - All three actions use `authProcedure({ required: true })`
- **Files changed:**
  - `src/server/features/auth/auth.schemas.ts` — added totpDisableSchema, totpRegenerateCodesSchema
  - `src/server/features/auth/controllers/auth.controller.ts` — added totpDisable, totpRegenerateCodes, totpListDevices actions + schema imports
- **Learnings for future iterations:**
  - totpDisable accepts both TOTP codes (6 digits regex) and recovery codes as second factor — first tries TOTP, falls back to recovery code comparison
  - Pattern for invalidating recovery codes: `updateMany({ where: { userId, usedAt: null }, data: { usedAt: new Date() } })` instead of deleting, preserves audit trail
  - Pre-existing TS errors (~60+) continue in unrelated files — confirmed zero new errors from US-006 changes
---

## 2026-03-13 ~08:00 UTC-3 - US-007
- **Implemented:** UI de configuracao 2FA na pagina de seguranca do usuario
- **Changes:**
  - Added `TwoFactorSection` component to `/user/seguranca/page.tsx`
  - Disabled state: Card with ShieldOff icon + description + "Ativar 2FA" button
  - Setup wizard Dialog with 3 steps:
    - Step 1: QR code image (base64) + secret in monospace with show/hide toggle + copy button
    - Step 2: InputOTP 6-digit verification
    - Step 3: Recovery codes in 2x4 grid + copy all + download .txt
  - Enabled state: Card with ShieldCheck icon + "Ativo" badge + "Desabilitar 2FA" and "Regenerar Codigos" buttons
  - Disable 2FA modal: password input + TOTP/recovery code input
  - Regenerate codes modal: TOTP code verification then shows new codes grid
  - Loading states, error handling with Alert components, toast notifications throughout
  - Uses `apiFetch` helper with fetch API (same pattern as admin/security page)
  - API endpoints: `/api/v1/auth/totp/setup`, `/totp/verify`, `/totp/disable`, `/totp/regenerate-codes`, `/totp/devices`
- **Files changed:**
  - `src/app/user/seguranca/page.tsx` — added TwoFactorSection + updated header description
- **Learnings for future iterations:**
  - `/user/seguranca` page was untracked on this branch (exists only in worktree) — use `git add` explicitly
  - The page uses raw `fetch` with `credentials: 'include'` (not the Igniter `api.*` client) because it's a standalone `'use client'` page
  - `apiFetch` helper pattern (from admin/security) is clean and reusable — wraps fetch with credentials, JSON headers, and error extraction
  - Auth controller base path is `/auth`, so TOTP endpoints are at `/api/v1/auth/totp/*`
  - `me` endpoint does NOT return `twoFactorEnabled` — use `totpListDevices` to check 2FA status (verified devices = 2FA enabled)
  - InputOTP component from `@/client/components/ui/input-otp` — uses `input-otp` library, 6 slots in one group
---

## 2026-03-13 ~09:00 UTC-3 - US-008
- **Implemented:** UI de 2FA challenge no login
- **Changes:**
  - Created `TwoFactorChallenge` component in `src/client/components/auth/two-factor-challenge.tsx`
  - TOTP mode: InputOTP 6-digit with auto-submit on complete, attempts remaining counter
  - Recovery mode: text input for 8-char recovery code, warnings for low remaining codes
  - Toggle between TOTP and recovery modes with clear state reset
  - "Voltar ao login" cancel button
  - Modified `src/client/components/auth/login-otp-form.tsx`: added twoFactorChallengeId state, detects requiresTwoFactor in both phone OTP and email OTP responses, renders TwoFactorChallenge inline
  - Modified `src/app/(auth)/google-callback/page.tsx`: detects requiresTwoFactor from googleCallback response, shows TwoFactorChallenge with logo header
  - Modified `src/app/(auth)/login/verify-magic/LoginVerifyMagicClient.tsx`: detects requiresTwoFactor from verifyMagicLink response, shows TwoFactorChallenge
  - All flows redirect to onboarding/admin/integracoes based on user role after successful 2FA
- **Files changed:**
  - `src/client/components/auth/two-factor-challenge.tsx` — new component
  - `src/client/components/auth/login-otp-form.tsx` — added 2FA detection + render
  - `src/app/(auth)/google-callback/page.tsx` — added 2FA detection + render
  - `src/app/(auth)/login/verify-magic/LoginVerifyMagicClient.tsx` — added 2FA detection + render
- **Learnings for future iterations:**
  - TOTP challenge endpoint is `/api/v1/auth/totp-challenge` (NOT `/totp/challenge` — no nested slash)
  - Recovery endpoint is `/api/v1/auth/totp-recovery` (same pattern)
  - Error responses from challenge/recovery include `code` field (CHALLENGE_EXHAUSTED, INVALID_CHALLENGE, INVALID_CODE) and `attemptsRemaining`
  - Recovery success response includes `remainingCodes` and `warning` fields for low code warnings
  - When integrating 2FA into Google callback, need to import Image/Link for logo header layout
---

## 2026-03-13 ~10:00 UTC-3 - US-009
- **Implemented:** Cloudflare Turnstile backend procedure
- **Changes:**
  - Created `src/server/features/auth/procedures/turnstile.procedure.ts`
  - Procedure validates `cf-turnstile-response` token from request body via Cloudflare siteverify API
  - Fail-open: no TURNSTILE_SECRET_KEY = skip validation (dev), timeout/network error = accept request
  - Fail-closed: production with missing token = 403 `bot_detected`, Cloudflare success=false = 403
  - 5s timeout using AbortController on fetch to Cloudflare
  - Sends `secret`, `response`, `remoteip` (from x-forwarded-for/x-real-ip) to Cloudflare
  - Applied `use: [turnstileProcedure()]` to 5 actions: login, register, signupOTP, forgotPassword, loginOTPPhone
  - Imported turnstileProcedure in auth.controller.ts
- **Files changed:**
  - `src/server/features/auth/procedures/turnstile.procedure.ts` — new file
  - `src/server/features/auth/controllers/auth.controller.ts` — added import + use arrays on 5 actions
- **Learnings for future iterations:**
  - Igniter procedures follow pattern: `igniter.procedure({ name, handler: async (options, ctx) => {} })` returning `{}` for pass-through or `Response.json()` for rejection
  - Actions without `use` array can have one added — just add `use: [procedure()]` to the mutation/query config
  - Cloudflare Turnstile siteverify uses `application/x-www-form-urlencoded` content type, not JSON
  - Pre-existing TS errors (~60+) remain in unrelated files — no new errors from US-009
---

## 2026-03-13 ~11:00 UTC-3 - US-010
- **Implemented:** Cloudflare Turnstile — React frontend widget
- **Changes:**
  - Installed `@marsidev/react-turnstile` package (--legacy-peer-deps)
  - Created `TurnstileWidget` component in `src/client/components/auth/turnstile-widget.tsx`
  - Mode: managed (invisible for humans, visual challenge only if suspicious), theme: auto, size: flexible
  - Only renders when `NEXT_PUBLIC_TURNSTILE_SITE_KEY` env var is set (returns null otherwise)
  - On error/expiry: shows toast via `sonner` and auto-resets widget via ref
  - Integrated in 4 forms:
    - `src/client/components/auth/login-form-final.tsx` — phone (raw fetch) + email (Igniter api) flows
    - `src/client/components/auth/signup-form.tsx` — phone (raw fetch) + email (Igniter api) flows
    - `src/client/components/auth/signup-otp-form.tsx` — resend OTP flow (Igniter api)
    - `src/app/(auth)/forgot-password/page.tsx` — forgot password flow (Igniter api)
  - Token sent as `cf-turnstile-response` field in body of all form submissions
  - For Igniter typed API calls: used `as any` cast since `cf-turnstile-response` is extracted by turnstile procedure, not part of Zod body schema
- **Files changed:**
  - `package.json` / `package-lock.json` — added @marsidev/react-turnstile
  - `src/client/components/auth/turnstile-widget.tsx` — new component
  - `src/client/components/auth/login-form-final.tsx` — added TurnstileWidget + turnstileToken state
  - `src/client/components/auth/signup-form.tsx` — added TurnstileWidget + turnstileToken state
  - `src/client/components/auth/signup-otp-form.tsx` — added TurnstileWidget + turnstileToken state
  - `src/app/(auth)/forgot-password/page.tsx` — added TurnstileWidget + turnstileToken state
- **Learnings for future iterations:**
  - Igniter typed API (`api.auth.*.mutate`) enforces strict body types from Zod schemas — extra fields like `cf-turnstile-response` need `as any` cast
  - `@marsidev/react-turnstile` exports `Turnstile` component + `TurnstileInstance` type for ref
  - Toast uses `sonner` library — `import { toast } from 'sonner'`
  - Widget ref has `.reset()` method to re-render on error/expiry
  - Pre-existing TS errors (~60+) remain in unrelated files — zero new errors from US-010
---

## 2026-03-13 ~12:00 UTC-3 - US-011
- **Implemented:** Schema de Custom Roles — model CustomRole + customRoleId no UserOrganization
- **Changes:**
  - Added `CustomRole` model to `prisma/schema.prisma` with: id (uuid), organizationId (FK), name, slug, description, permissions (Json), isSystem (Boolean default false), priority (Int), createdAt, updatedAt
  - Table mapped to `custom_roles` via `@@map`
  - Unique constraint: `@@unique([organizationId, slug])`
  - Index: `@@index([organizationId])`
  - Added `customRoleId` (String? FK) to `UserOrganization` with `onDelete: SetNull`
  - Added `@@index([customRoleId])` to UserOrganization
  - Added relation: Organization hasMany CustomRole (onDelete: Cascade)
  - Created migration `20260313210000_add_custom_roles` manually (shadow DB workaround)
  - Data migration SQL creates 3 system CustomRoles per existing org (master priority=3, manager priority=2, user priority=1) with permissions JSON matching PERMISSIONS_MATRIX
  - Data migration links existing UserOrganization records to their corresponding CustomRole by matching `role` string to `slug`
  - Applied with `prisma migrate deploy`
  - `prisma generate` successful
- **Files changed:**
  - `prisma/schema.prisma` — added CustomRole model + customRoleId field + relations
  - `prisma/migrations/20260313210000_add_custom_roles/migration.sql` — new migration with DDL + data migration
- **Learnings for future iterations:**
  - Permissions JSON structure mirrors PERMISSIONS_MATRIX: `{ "resource_name": ["action1", "action2", ...] }` using Resource enum values as keys and Action enum values in arrays
  - System roles use `isSystem=true` and cannot be edited/deleted by users (enforced at controller level in future US-012)
  - Priority maps to ROLE_HIERARCHY: master=3, manager=2, user=1
  - Pre-existing TS errors (~60+) remain in unrelated files — zero new errors from US-011
---

## 2026-03-13 ~13:00 UTC-3 - US-012
- **Implemented:** CRUD de Custom Roles — controller Igniter
- **Changes:**
  - Created `src/server/features/permissions/controllers/custom-roles.controller.ts` with 5 actions:
    - `list` (query): returns CustomRole[] for current org, ordered by priority DESC, includes `_count.userOrganizations`
    - `create` (mutation): validates name (3-50 chars), description, permissions (Json via z.nativeEnum), priority (1-2), auto-generates slug (kebab-case), checks uniqueness per org
    - `get` (query): returns single role with `_count.userOrganizations`, checks org ownership
    - `update` (mutation): edits name/description/permissions/priority, rejects if isSystem=true, regenerates slug if name changes with uniqueness check
    - `delete` (mutation): rejects system roles, rejects if users assigned (returns count), supports force=true + reassignToRoleId for atomic reassign + delete via $transaction
  - All actions use `authProcedure({ required: true })` and check `organizationRole === 'master'` or `user.role === 'admin'`
  - Permissions validated with `z.record(z.nativeEnum(Resource), z.array(z.nativeEnum(Action)))` ensuring only valid Resource/Action enum values
  - Registered `customRolesController` in `src/igniter.router.ts` as `customRoles` key
- **Files changed:**
  - `src/server/features/permissions/controllers/custom-roles.controller.ts` — new controller file
  - `src/igniter.router.ts` — added import + registration of customRolesController
- **Learnings for future iterations:**
  - `z.nativeEnum(Resource)` and `z.nativeEnum(Action)` provide strict validation against TypeScript enums — use for any enum-based JSON validation
  - `z.record(keySchema, valueSchema)` validates object keys and values — perfect for permissions JSON structure
  - Igniter controller pattern: `response.created()` for POST success, `response.success()` for GET/PUT/DELETE
  - Custom role slug generation: normalize NFD + strip accents + kebab-case — handles Portuguese characters
  - `@@unique([organizationId, slug])` maps to Prisma `findUnique({ where: { organizationId_slug: { organizationId, slug } } })`
  - Pre-existing TS errors (~60+) remain in unrelated files — zero new errors from US-012
---

## 2026-03-13 ~14:00 UTC-3 - US-013
- **Implemented:** Migrate permission system to use CustomRole from DB
- **Changes:**
  - Added in-memory cache (`Map<string, CacheEntry>`) with 5min TTL in `src/lib/auth/permissions.ts`
  - Added `getCustomRolePermissions(customRoleId, db)`: fetches from cache or DB, returns permissions JSON
  - Added `invalidateCustomRoleCache(customRoleId)` and `clearCustomRoleCache()` exports for cache management
  - Added `CustomRolePermissions` type and `CustomRoleContext` interface (id, slug, permissions, priority)
  - Updated `hasPermission()`: new optional 4th param `customRolePermissions` — if provided, checks against CustomRole permissions instead of PERMISSIONS_MATRIX
  - Updated `checkPermission()`: context type extended with optional `customRolePermissions` field, passed through to `hasPermission()`
  - Updated `authProcedure` in `auth.procedure.ts`:
    - Added `customRole: CustomRoleContext | null` to `AuthContext` type
    - After user lookup, resolves `customRoleId` from current org's `UserOrganization`
    - Calls `getCustomRolePermissions()` to fetch/cache permissions
    - Fetches slug + priority from DB for full `CustomRoleContext`
    - Injects `context.auth.customRole` in all return paths (null for unauthenticated/admin)
  - Updated `custom-roles.controller.ts`: calls `invalidateCustomRoleCache()` after create, update, and delete (both force+reassign and simple delete paths)
  - Zero breaking changes: all existing callers of `hasPermission(role, resource, action)` continue working unchanged
- **Files changed:**
  - `src/lib/auth/permissions.ts` — cache, types, updated hasPermission/checkPermission
  - `src/server/features/auth/procedures/auth.procedure.ts` — customRole context injection
  - `src/server/features/permissions/controllers/custom-roles.controller.ts` — cache invalidation on CRUD
- **Learnings for future iterations:**
  - `getCustomRolePermissions` takes `db` as param to avoid importing database service in a lib module (keeps it framework-agnostic)
  - The `organizations` include in authProcedure user query already includes `customRoleId` via the Prisma schema relation — no need to add explicit `select`
  - Cache invalidation should also cover the `reassignToRoleId` target role on force-delete, since users now point to a different role
  - Pre-existing TS errors (~60+) remain in unrelated files — zero new errors from US-013
---

## 2026-03-13 ~15:00 UTC-3 - US-014
- **Implemented:** UI de gerenciamento de Custom Roles
- **Changes:**
  - Created `/admin/roles` page (`src/app/admin/roles/page.tsx`) as 'use client' component
  - System roles displayed as read-only cards (3 per row) with member count + "Somente leitura" label
  - Custom roles displayed in table with name/slug, description, priority badge, member count, edit/delete actions
  - RoleFormDialog: create/edit form with name (3-50 chars), description (optional), priority selector (1-2), full permissions matrix
  - PermissionsMatrix component: table grid of Resources (14 rows) x Actions (6 columns: CRUD + List + Manage) with Checkbox components
  - Click resource name to toggle all actions for that resource
  - System roles open dialog in view-only mode (all inputs disabled, no save button)
  - DeleteRoleDialog: confirmation with member count warning, Select dropdown for reassignment role when members exist
  - Uses `apiFetch` helper (fetch + credentials: include + JSON headers) to call `/api/v1/custom-roles` endpoints
  - Empty state for custom roles with "Criar primeiro role" button
  - Error state with retry button
  - Loading state with Skeleton placeholders
  - Added "Roles" link with ShieldCheck icon to admin sidebar navigation in `app-sidebar.tsx`
- **Files changed:**
  - `src/app/admin/roles/page.tsx` — new page
  - `src/client/components/app-sidebar.tsx` — added ShieldCheck import + Roles menu item
- **Learnings for future iterations:**
  - Admin pages follow consistent pattern: header with SidebarTrigger + Breadcrumb, then content div with `p-8 pt-6`
  - Custom roles API returns `_count.userOrganizations` for member count — use `Array.isArray(json?.data)` guard for response parsing
  - DELETE endpoint expects JSON body with `force` and `reassignToRoleId` — standard fetch with method: DELETE + body works
  - Resources and Actions are string enums from `@/lib/auth/permissions` — mirror them as constant arrays in frontend for the matrix UI
  - Pre-existing TS errors (~60+) remain in unrelated files — zero new errors from US-014
---

## 2026-03-13 ~16:00 UTC-3 - US-015
- **Implemented:** Schema e verificação DNS/Email para Domínios Verificados
- **Changes:**
  - Added `VerifiedDomain` model to `prisma/schema.prisma`: id, organizationId (FK), domain, verificationMethod (DNS_TXT/EMAIL), verificationToken, verifiedAt, defaultRoleId (FK CustomRole), autoJoin, createdAt
  - Unique constraint: `@@unique([organizationId, domain])`, indexes on organizationId and domain
  - Added relation from Organization (hasMany VerifiedDomain) and CustomRole (hasMany VerifiedDomain via defaultRoleId)
  - Created migration `20260313220000_add_verified_domains` manually (shadow DB workaround)
  - Applied with `prisma migrate deploy`, `prisma generate` successful
  - Created `src/server/features/verified-domains/controllers/verified-domains.controller.ts` with 6 actions:
    - `addDomain`: POST, generates `quayer-verify=<64hexchars>` token, sends email to admin@domain for EMAIL method
    - `verifyDomainDns`: POST, uses `dns.resolveTxt()` to find TXT record containing token
    - `verifyDomainEmail`: POST, validates 6-digit code against in-memory store (30min expiry)
    - `list`: GET, returns all domains for org with defaultRole include
    - `updateDomain`: PUT /:id, toggle autoJoin and set defaultRoleId (requires verified domain)
    - `removeDomain`: DELETE /:id, deletes domain + cleans up email verification codes
  - Blocklist: 15+ generic email domains (gmail, hotmail, outlook, yahoo, live, icloud, protonmail, etc.)
  - Rate limit: in-memory 10 verifications/hour/org with sliding window
  - All actions protected with `authProcedure({ required: true })`, master/admin-only checks
  - Registered `verifiedDomainsController` in `src/igniter.router.ts`
- **Files changed:**
  - `prisma/schema.prisma` — added VerifiedDomain model + relations to Organization and CustomRole
  - `prisma/migrations/20260313220000_add_verified_domains/migration.sql` — new migration
  - `src/server/features/verified-domains/controllers/verified-domains.controller.ts` — new controller
  - `src/igniter.router.ts` — registered verifiedDomainsController
- **Learnings for future iterations:**
  - Igniter `response` object does NOT have `tooManyRequests` method — use `Response.json({}, { status: 429 })` directly for 429 responses
  - `dns.resolveTxt()` returns `string[][]` (array of arrays) — flatten with `.flat()` before searching
  - In-memory Maps are used for short-lived verification state (email codes, rate limits) — adequate for single-instance deployments
  - VerifiedDomain.defaultRoleId FK points to CustomRole for auto-join role assignment
  - Pre-existing TS errors (~60+) remain in unrelated files — zero new errors from US-015
---
