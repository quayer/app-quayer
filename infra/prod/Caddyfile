{
	admin off
}

(cf_origin_tls) {
	tls /etc/ssl/cloudflare/quayer-origin.pem /etc/ssl/cloudflare/quayer-origin.key
}

(security_headers) {
	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
		X-Frame-Options "SAMEORIGIN"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		Permissions-Policy "geolocation=(), microphone=(), camera=()"
		-Server
	}
}

app.quayer.com {
	import cf_origin_tls
	import security_headers
	reverse_proxy localhost:3000 {
		header_up Host {host}
		header_up X-Real-IP {remote_host}
		header_up X-Forwarded-Proto https
	}
	log {
		output file /var/log/caddy/app.quayer.com.log
		format json
	}
}

flows.quayer.com {
	import cf_origin_tls
	import security_headers
	reverse_proxy localhost:5678 {
		header_up Host {host}
		header_up X-Real-IP {remote_host}
		header_up X-Forwarded-Proto https
	}
	log {
		output file /var/log/caddy/flows.quayer.com.log
		format json
	}
}

chat.quayer.com {
	import cf_origin_tls
	import security_headers
	reverse_proxy localhost:4000 {
		header_up Host {host}
		header_up X-Real-IP {remote_host}
		header_up X-Forwarded-Proto https
	}
	log {
		output file /var/log/caddy/chat.quayer.com.log
		format json
	}
}

draw.quayer.com {
	import cf_origin_tls
	import security_headers
	reverse_proxy localhost:3030 {
		header_up Host {host}
		header_up X-Real-IP {remote_host}
		header_up X-Forwarded-Proto https
	}
	log {
		output file /var/log/caddy/draw.quayer.com.log
		format json
	}
}

supabase.quayer.com {
	import cf_origin_tls
	import security_headers
	reverse_proxy localhost:54323 {
		header_up Host {host}
		header_up X-Real-IP {remote_host}
		header_up X-Forwarded-Proto https
	}
	log {
		output file /var/log/caddy/supabase.quayer.com.log
		format json
	}
}
