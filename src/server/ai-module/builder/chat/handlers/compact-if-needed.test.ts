/**
 * Tests for compactIfNeeded.
 */

import { describe, it } from 'vitest'

describe('compactIfNeeded', () => {
  it.todo('returns compacted=false when history is under threshold')
  it.todo('returns compacted=true and summary when shouldCompact is true')
  it.todo('returns exhausted=true on ContextBudgetExhaustedError')
  it.todo('swallows non-exhausted errors and returns original messages')
})
